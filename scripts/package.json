{
  "name": "pos-supabase-push",
  "private": true,
  "description": "Run once to push Pixel POS schema to Supabase",
  "scripts": {
    "push-db": "node push_db.js"
  },
  "dependencies": {
    "pg": "^8.11.3"
  }
}
